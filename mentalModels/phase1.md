User → submits task
        ↓
Backend → filters (AI)
        ↓
Oracle → validates shortlist
        ↓
Experts → score + sign
        ↓
Backend → aggregates (median)
        ↓
Oracle → verifies winners
        ↓
Signer → signs winners
        ↓
User → submits signature
        ↓
Smart contract → verifies → mints token